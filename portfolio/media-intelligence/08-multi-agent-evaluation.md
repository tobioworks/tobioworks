# Multi-agent evaluation pattern

Portfolio-safe draft.

Different roles can be evaluated independently: discovery for coverage, verification for evidence integrity, editorial synthesis for usefulness, and adversarial review for failure discovery.

Model diversity is treated as evidence only when different models actually execute; otherwise the system reports role separation without overstating independence.
